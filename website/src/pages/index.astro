---
import '../styles/global.css'

const GITHUB = 'https://github.com/everydev1618/govega'
const DOCS   = `${GITHUB}#readme`

const features = [
  {
    glyph: '⟳',
    color: 'text-indigo-400',
    title: 'Erlang-style supervision',
    desc: 'OneForOne, OneForAll, RestForOne strategies. Processes restart automatically with exponential backoff. Process linking and monitors included.',
  },
  {
    glyph: '⬡',
    color: 'text-cyan-400',
    title: 'Go library + YAML DSL',
    desc: 'Full programmatic control via Go, or define multi-agent workflows in YAML with if/else, for-each, parallel, and try/catch.',
  },
  {
    glyph: '◈',
    color: 'text-purple-400',
    title: 'Built-in web dashboard',
    desc: 'Chat UI, process explorer, spawn tree, cost dashboard, SSE event stream, and REST API. Run vega serve and open localhost:3001.',
  },
  {
    glyph: '⌬',
    color: 'text-emerald-400',
    title: 'MCP server support',
    desc: 'Connect any Model Context Protocol server. Tools are registered automatically as servername__toolname and available to agents.',
  },
  {
    glyph: '◎',
    color: 'text-rose-400',
    title: 'Smart error classification',
    desc: '7 error classes (RateLimit, Overloaded, Timeout…). Rate-limited calls get retried with backoff. Auth errors are not retried.',
  },
  {
    glyph: '≋',
    color: 'text-amber-300',
    title: 'Real-time streaming',
    desc: 'Token-by-token SSE streaming with inline tool call panels. ChatStream API makes rich UIs straightforward to build.',
  },
]

const ocVega = [
  // [label, openclaw, vega, note?]
  ['Target use',            'Personal AI assistant',    'Backend agent services',       ''],
  ['Language / runtime',    'TypeScript · Node.js',     'Go',                           ''],
  ['LLM providers',         'Claude, OpenAI, Gemini, Bedrock…', 'Anthropic only (for now)', 'vega-gap'],
  ['Messaging channels',    '10+ (WhatsApp, Telegram, Slack, Discord, Signal, iMessage, Matrix…)', 'Telegram', 'oc-strength'],
  ['Device integration',    'iOS · Android · macOS apps', '—',                          'oc-strength'],
  ['Browser automation',    '✓ Playwright',             '—',                            'oc-strength'],
  ['Fault tolerance',       'Session recovery',         'Erlang-style supervision trees', 'vega-strength'],
  ['Process linking',       '—',                        '✓ Links · monitors · trap-exit', 'vega-strength'],
  ['Smart error retry',     '—',                        '✓ 7 error classes',            'vega-strength'],
  ['Per-process cost caps', '—',                        '✓ Budget per agent',           'vega-strength'],
  ['Workflow engine',       '—',                        '✓ YAML DSL with if/else, loops, parallel', 'vega-strength'],
  ['Built-in dashboard',    '—',                        '✓ Process explorer, spawn tree, cost graph', 'vega-strength'],
  ['Multi-agent teams',     'Via session tools',        '✓ Process groups + shared blackboard', 'vega-strength'],
  ['MCP server support',    'Via mcporter bridge',      '✓ Native client',              ''],
  ['Vector memory',         '✓ LanceDB plugin',         '—',                            'vega-gap'],
  ['Multi-host clustering', '—',                        '—',                            ''],
]

const vegaGaps = [
  ['Anthropic only', 'No OpenAI, Gemini, or local model support yet. The LLM interface makes this possible to add, but it doesn\'t exist today.'],
  ['In-memory process registry', 'All process state lives in a single process. No clustering, no multi-host coordination, no hand-off across restarts.'],
  ['Crude token estimation', 'Context trimming uses ~4 chars per token — not actual tokenisation. Works well enough in practice, but it\'s an approximation.'],
  ['Limited DSL expressions', 'The YAML expression language has no custom functions and global-only variable scope. Complex logic still needs Go code.'],
  ['Basic sandboxing', 'Tool sandboxing is path-based only. No memory limits, I/O quotas, or capability restrictions beyond file paths.'],
  ['No companion apps', 'No mobile or desktop apps. Vega is a server-side library and CLI, not an end-user product.'],
]
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Vega — Fault-tolerant AI agent orchestration for Go</title>
  <meta name="description" content="Build reliable AI agent systems with Erlang-style supervision, YAML DSL, and a built-in web dashboard." />
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>★</text></svg>" />
</head>

<body class="antialiased">

  <!-- Nav -->
  <header class="sticky top-0 z-50 border-b border-white/5 bg-space-950/80 backdrop-blur-sm">
    <div class="max-w-5xl mx-auto px-6 h-14 flex items-center justify-between">
      <span class="font-mono font-semibold text-sm bg-gradient-to-r from-indigo-400 to-purple-400 bg-clip-text text-transparent">
        Vega
      </span>
      <nav class="flex items-center gap-6 text-sm text-slate-400">
        <a href="#install"  class="hover:text-white transition-colors">Install</a>
        <a href="#features" class="hover:text-white transition-colors">Features</a>
        <a href="#quickstart" class="hover:text-white transition-colors">Quick Start</a>
        <a href={GITHUB} target="_blank" class="hover:text-white transition-colors flex items-center gap-1.5">
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z"/>
          </svg>
          GitHub
        </a>
      </nav>
    </div>
  </header>

  <!-- Hero -->
  <section class="max-w-5xl mx-auto px-6 pt-24 pb-20 text-center">
    <!-- ASCII star -->
    <div class="inline-block mb-10 animate-pulse-slow" aria-hidden="true">
      <pre class="font-mono text-base leading-snug select-none text-left inline-block"><span class="star-dot">        ·   </span><span class="star-tip">✦</span><span class="star-dot">   ·
</span><span class="star-arm-ne">         \  </span><span class="star-arm-n">│</span><span class="star-arm-ne">  /
          \ </span><span class="star-arm-n">│</span><span class="star-arm-ne"> /
</span><span class="star-dot">  · </span><span class="star-arm-w">✦ ─────</span><span class="star-center"> ★ </span><span class="star-arm-e">───── ✦</span><span class="star-dot"> ·
</span><span class="star-arm-sw">          / </span><span class="star-arm-s">│</span><span class="star-arm-sw"> \
         /  </span><span class="star-arm-s">│</span><span class="star-arm-sw">  \
</span><span class="star-dot">        ·   </span><span class="star-tip">✦</span><span class="star-dot">   ·</span></pre>
    </div>

    <h1 class="text-5xl font-mono font-semibold tracking-tight mb-4 bg-gradient-to-br from-indigo-300 via-purple-300 to-cyan-300 bg-clip-text text-transparent">
      Vega
    </h1>
    <p class="text-xl text-slate-300 mb-3 max-w-2xl mx-auto">
      Fault-tolerant AI agent orchestration for Go
    </p>
    <p class="text-slate-500 mb-10 max-w-xl mx-auto">
      Erlang-style supervision trees, a YAML DSL for non-programmers, and a built-in web dashboard with real-time streaming.
    </p>

    <div class="flex items-center justify-center gap-4 flex-wrap">
      <a href={GITHUB} target="_blank"
         class="px-5 py-2.5 rounded-lg bg-indigo-600 hover:bg-indigo-500 text-white font-mono text-sm transition-colors">
        View on GitHub →
      </a>
      <a href="#quickstart"
         class="px-5 py-2.5 rounded-lg border border-white/15 hover:border-white/30 text-slate-300 hover:text-white font-mono text-sm transition-colors">
        Quick Start
      </a>
    </div>
  </section>

  <!-- Install -->
  <section id="install" class="max-w-5xl mx-auto px-6 pb-20">
    <p class="section-label text-center">Install</p>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 max-w-2xl mx-auto">
      <div class="code-block">
        <p class="text-slate-500 text-xs mb-2"># CLI via Homebrew</p>
        <p class="text-emerald-400">brew install <span class="text-white">everydev1618/tap/vega</span></p>
      </div>
      <div class="code-block">
        <p class="text-slate-500 text-xs mb-2"># Go library</p>
        <p class="text-emerald-400">go get <span class="text-white">github.com/everydev1618/govega</span></p>
      </div>
    </div>
  </section>

  <!-- Features -->
  <section id="features" class="max-w-5xl mx-auto px-6 pb-24">
    <p class="section-label text-center">Features</p>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      {features.map(f => (
        <div class="feature-card">
          <div class={`text-2xl mb-3 font-mono ${f.color}`}>{f.glyph}</div>
          <h3 class="font-semibold text-white mb-2">{f.title}</h3>
          <p class="text-sm text-slate-400 leading-relaxed">{f.desc}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- Quick Start -->
  <section id="quickstart" class="max-w-5xl mx-auto px-6 pb-24">
    <p class="section-label text-center">Quick Start</p>
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-start">

      <!-- Go library -->
      <div>
        <h3 class="font-mono text-white font-semibold mb-3">Go library</h3>
        <div class="code-block text-xs leading-relaxed">
          <p><span class="text-slate-500">package</span> <span class="text-cyan-400">main</span></p>
          <p class="mt-2"><span class="text-slate-500">import</span> (</p>
          <p class="pl-4 text-amber-300">"context"</p>
          <p class="pl-4 text-amber-300">"fmt"</p>
          <p class="pl-4 text-amber-300">"github.com/everydev1618/govega"</p>
          <p class="pl-4 text-amber-300">"github.com/everydev1618/govega/llm"</p>
          <p>)</p>
          <p class="mt-2"><span class="text-slate-500">func</span> <span class="text-indigo-400">main</span>() &#123;</p>
          <p class="pl-4">llm  := <span class="text-cyan-400">llm</span>.<span class="text-indigo-400">NewAnthropic</span>()</p>
          <p class="pl-4">orch := <span class="text-cyan-400">vega</span>.<span class="text-indigo-400">NewOrchestrator</span>(</p>
          <p class="pl-8"><span class="text-cyan-400">vega</span>.<span class="text-indigo-400">WithLLM</span>(llm),</p>
          <p class="pl-4">)</p>
          <p class="pl-4 mt-2">agent := <span class="text-cyan-400">vega</span>.Agent&#123;</p>
          <p class="pl-8">Name:   <span class="text-amber-300">"assistant"</span>,</p>
          <p class="pl-8">Model:  <span class="text-amber-300">"claude-sonnet-4-20250514"</span>,</p>
          <p class="pl-8">System: <span class="text-cyan-400">vega</span>.<span class="text-indigo-400">StaticPrompt</span>(<span class="text-amber-300">"You are helpful."</span>),</p>
          <p class="pl-4">&#125;</p>
          <p class="pl-4 mt-2">proc, _ := orch.<span class="text-indigo-400">Spawn</span>(agent)</p>
          <p class="pl-4">resp, _ := proc.<span class="text-indigo-400">Send</span>(context.<span class="text-indigo-400">Background</span>(), <span class="text-amber-300">"Hello!"</span>)</p>
          <p class="pl-4">proc.<span class="text-indigo-400">Complete</span>(resp)</p>
          <p class="pl-4"><span class="text-cyan-400">fmt</span>.<span class="text-indigo-400">Println</span>(resp)</p>
          <p>&#125;</p>
        </div>
      </div>

      <!-- YAML DSL -->
      <div>
        <h3 class="font-mono text-white font-semibold mb-3">YAML DSL</h3>
        <div class="code-block text-xs leading-relaxed mb-4">
          <p><span class="text-rose-400">name</span>: <span class="text-white">MyTeam</span></p>
          <p class="mt-2"><span class="text-rose-400">agents</span>:</p>
          <p class="pl-4"><span class="text-cyan-400">researcher</span>:</p>
          <p class="pl-8"><span class="text-rose-400">model</span>: <span class="text-white">claude-sonnet-4-20250514</span></p>
          <p class="pl-8"><span class="text-rose-400">system</span>: <span class="text-white">You research topics thoroughly.</span></p>
          <p class="pl-4"><span class="text-cyan-400">writer</span>:</p>
          <p class="pl-8"><span class="text-rose-400">model</span>: <span class="text-white">claude-sonnet-4-20250514</span></p>
          <p class="pl-8"><span class="text-rose-400">system</span>: <span class="text-white">You write clear summaries.</span></p>
          <p class="mt-2"><span class="text-rose-400">workflows</span>:</p>
          <p class="pl-4"><span class="text-cyan-400">report</span>:</p>
          <p class="pl-8"><span class="text-rose-400">steps</span>:</p>
          <p class="pl-10">- <span class="text-cyan-400">researcher</span>:</p>
          <p class="pl-14"><span class="text-rose-400">send</span>: <span class="text-white">"Research &#123;&#123;topic&#125;&#125;"</span></p>
          <p class="pl-14"><span class="text-rose-400">save</span>: <span class="text-white">findings</span></p>
          <p class="pl-10">- <span class="text-cyan-400">writer</span>:</p>
          <p class="pl-14"><span class="text-rose-400">send</span>: <span class="text-white">"Summarise: &#123;&#123;findings&#125;&#125;"</span></p>
        </div>
        <div class="code-block text-xs">
          <p class="text-slate-500"># Run it</p>
          <p class="text-emerald-400">vega run <span class="text-white">team.vega.yaml</span> \</p>
          <p class="text-emerald-400 pl-4">--workflow report \</p>
          <p class="text-emerald-400 pl-4">--task <span class="text-amber-300">"topic=Go concurrency"</span></p>
        </div>
      </div>

    </div>
  </section>

  <!-- Why Vega -->
  <section class="max-w-5xl mx-auto px-6 pb-24">
    <p class="section-label text-center">Why Vega?</p>

    <!-- Framing -->
    <p class="text-slate-400 text-sm leading-relaxed max-w-2xl mx-auto text-center mb-12">
      A fair comparison requires an honest framing. Vega and
      <a href="https://openclaw.dev" target="_blank" class="text-indigo-400 hover:text-indigo-300 transition-colors">OpenClaw</a>
      are both AI agent projects from the same author, but they solve fundamentally different problems.
      Neither is a substitute for the other.
    </p>

    <!-- Positioning cards -->
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-12">
      <div class="rounded-xl border border-white/10 bg-space-900 p-6">
        <p class="font-mono text-xs text-slate-500 mb-1">OpenClaw</p>
        <p class="font-semibold text-white mb-3">A personal AI assistant for end users</p>
        <p class="text-sm text-slate-400 leading-relaxed">
          OpenClaw runs as a daemon on your devices and connects to messaging channels you already use —
          WhatsApp, Telegram, Slack, Discord, iMessage, and more. It pairs with your phone and Mac so the
          agent can take real-world actions: browse the web, run shell commands, control your screen.
          It is built for one person, local-first, and privacy-centric.
        </p>
      </div>
      <div class="rounded-xl border border-indigo-500/30 bg-space-900 p-6">
        <p class="font-mono text-xs text-indigo-400 mb-1">Vega</p>
        <p class="font-semibold text-white mb-3">A framework for building backend agent services</p>
        <p class="text-sm text-slate-400 leading-relaxed">
          Vega is a Go library and CLI for developers who need to run multi-agent systems reliably in
          production. Its core concern is fault tolerance: processes restart automatically, errors are
          classified and retried intelligently, costs are tracked per agent, and everything is observable
          through a built-in dashboard. It is not an end-user product.
        </p>
      </div>
    </div>

    <!-- Detailed comparison table -->
    <div class="overflow-x-auto rounded-xl border border-white/10 mb-12">
      <table class="w-full text-sm font-mono">
        <thead>
          <tr class="border-b border-white/10 bg-space-800">
            <th class="text-left px-5 py-3 text-slate-400 font-normal w-1/3">Dimension</th>
            <th class="text-left px-4 py-3 text-slate-400 font-normal w-1/3">OpenClaw</th>
            <th class="text-left px-4 py-3 text-indigo-400 font-semibold w-1/3">Vega</th>
          </tr>
        </thead>
        <tbody>
          {ocVega.map(([label, oc, vega, cls], i) => (
            <tr class={`border-b border-white/5 ${i % 2 === 0 ? 'bg-space-900' : 'bg-space-950'}`}>
              <td class="px-5 py-3 text-slate-400 align-top">{label}</td>
              <td class={`px-4 py-3 align-top leading-snug ${cls === 'oc-strength' ? 'text-emerald-400' : 'text-slate-300'}`}>
                {oc === '—' ? <span class="text-slate-600">—</span> : oc}
              </td>
              <td class={`px-4 py-3 align-top leading-snug ${cls === 'vega-strength' ? 'text-indigo-300' : cls === 'vega-gap' ? 'text-amber-400' : 'text-slate-300'}`}>
                {vega === '—' ? <span class="text-slate-600">—</span> : vega}
              </td>
            </tr>
          ))}
        </tbody>
      </table>
    </div>

    <!-- Vega current gaps -->
    <div>
      <p class="font-mono text-sm text-slate-400 mb-2 text-center">Current limitations in Vega</p>
      <p class="text-slate-500 text-xs text-center mb-6 max-w-xl mx-auto">
        These are real gaps, not roadmap promises. We'd rather you know them upfront than discover them after adopting the project.
      </p>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
        {vegaGaps.map(([title, desc]) => (
          <div class="rounded-lg border border-amber-500/15 bg-amber-500/5 px-5 py-4">
            <p class="font-mono text-xs text-amber-400 mb-1">{title}</p>
            <p class="text-xs text-slate-400 leading-relaxed">{desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="border-t border-white/5 py-10">
    <div class="max-w-5xl mx-auto px-6 flex flex-col md:flex-row items-center justify-between gap-4 text-sm text-slate-500">
      <div class="flex items-center gap-2 font-mono">
        <span class="text-amber-300">★</span>
        <span class="bg-gradient-to-r from-indigo-400 to-purple-400 bg-clip-text text-transparent font-semibold">Vega</span>
        <span>— MIT License</span>
      </div>
      <nav class="flex items-center gap-6">
        <a href={GITHUB} target="_blank" class="hover:text-white transition-colors">GitHub</a>
        <a href={`${GITHUB}/tree/main/docs`} target="_blank" class="hover:text-white transition-colors">Docs</a>
        <a href={`${GITHUB}/tree/main/examples`} target="_blank" class="hover:text-white transition-colors">Examples</a>
      </nav>
    </div>
  </footer>

</body>
</html>
